/******/ (() => { // webpackBootstrap
/******/ 	"use strict";

;// ./packages/core/makeup-prevent-scroll-keys/dist/mjs/index.js
const SCROLL_KEYS = /* @__PURE__ */ new Set([
  " ",
  "PageUp",
  "PageDown",
  "End",
  "Home",
  "ArrowLeft",
  "ArrowUp",
  "ArrowRight",
  "ArrowDown"
]);
const onKeyDown = (e) => {
  if (SCROLL_KEYS.has(e.key)) {
    e.preventDefault();
  }
};
const add = (el) => {
  el.addEventListener("keydown", onKeyDown);
};
const remove = (el) => {
  el.removeEventListener("keydown", onKeyDown);
};


;// ./docs/core/makeup-prevent-scroll-keys/index.compiled.js

const widgetEl = document.querySelector(".widget");
const scrollOutput = document.getElementById("scroll-y");
const toggleBtn = document.getElementById("toggle");
let enabled = true;
add(widgetEl);
window.addEventListener("scroll", () => {
  scrollOutput.textContent = Math.round(window.scrollY);
});
toggleBtn.addEventListener("click", () => {
  if (enabled) {
    remove(widgetEl);
    toggleBtn.textContent = "Enable prevention";
  } else {
    add(widgetEl);
    toggleBtn.textContent = "Disable prevention";
  }
  enabled = !enabled;
});
/******/ })()
;
//# sourceMappingURL=index.min.js.map